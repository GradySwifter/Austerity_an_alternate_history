title: Left-wing Split
new-page: true 
subtitle: The Left's disagreements with our policies have grown too severe.
tags: event
view-if: left_dissent >= 60
on-arrival: left_split = 1; left_strength = left_strength/2; workers_z -= 5; unemployed_z -= 5; workers_other += 2; workers_dnvp +=2; workers_spd += 3; unemployed_spd +=2; n_advisors -= 1 if wirth_advisor == 1; wirth_advisor = 0; rb_strength -= 300; spd_relation += 5; rb_militancy -= 0.05 if rb_militancy >= 0.1; n_advisors -= 1 if kaiser_advisor == 1; kaiser_advisor = 0; n_advisors -= 1 if joos_advisor == 1; joos_advisor = 0;
max-visits: 1

= Left-wing members split

Our policy choices have greatly offended the left, and many of their leaders have split. Some of them have joined the SPD, while many have defected towards minor left-leaning parties, and a few have even joined the Christian Socialist wing of the DNVP. 

However, as our Left has been somewhat sidelined in recent years, this was not a terrible loss.

